"use strict"; // treat all JS code as newer version

// dataTypes in JS:

let name = "AHMAD";
let age = 23;
let isLoggedIn = true;

/*
premmitive Data Types:

numner => 2 power 53
bigInt => greater than number used in special case where our number is big e.g stock Market
string => ""
boolean => true/false
null => standalone value 
undefine => variable is declare but value is not assign
symbol => unique

Non premmitive Data Types:

object =>
*/
